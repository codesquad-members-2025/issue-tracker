package elbin_bank.issue_tracker.issue.infrastructure.query.projection;

public record UserInfoProjection(long id,
                                 String nickname,
                                 String profileImage) {
}
