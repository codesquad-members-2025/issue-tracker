package elbin_bank.issue_tracker.label.application.query.dto;

public record LabelsDto(Long id,
                        String name,
                        String color,
                        String description) {
}
